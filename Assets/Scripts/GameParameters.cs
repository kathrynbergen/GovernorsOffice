using System.Collections.Generic;
using UnityEngine;

public static class GameParameters
{
    /*
    public static Hexagon[] AllHexes;
    
    public static Dictionary<Hexagon, List<Hexagon>> AdjacentHexMap = new Dictionary<Hexagon, List<Hexagon>>();
    
    public static int CurrentLevel = 1;

    public static float HorizontalDistToHexagon = 1.2f;
    public static float VerticalDistToHexagon = 0.8856f;

    public static Vector3[] DistancesToAdjacentHexagons =
    {
        new Vector3(HorizontalDistToHexagon/2,VerticalDistToHexagon,0),
        new Vector3(HorizontalDistToHexagon,0,0),
        new Vector3(HorizontalDistToHexagon/2,-VerticalDistToHexagon,0),
        new Vector3(-HorizontalDistToHexagon/2,-VerticalDistToHexagon,0),
        new Vector3(-HorizontalDistToHexagon,0,0),
        new Vector3(-HorizontalDistToHexagon/2,VerticalDistToHexagon,0)
    };

    public static void SetupNeighbors()
    {
        // example for 3 hexes
        Hexagon hex0 = AllHexes[0];
        Hexagon hex1 = AllHexes[1];
        Hexagon hex8 = AllHexes[8];
        Hexagon hex9 = AllHexes[9];

        AdjacentHexMap[hex0] = new List<Hexagon> { hex1, hex8, hex9 };
        AdjacentHexMap[hex1] = new List<Hexagon> { hex0 };
        // ...repeat for all hexes
    }
    */
}
